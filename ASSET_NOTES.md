# Asset Notes

- `assets/images/kunkun_menu_background.png` is the exact pixel-art image supplied by the user as `codex-clipboard-64d085dc-ffec-4301-a3d9-b1f23f9ace2c.png`.
- `assets/audio/menu_theme.mp3` is the exact trimmed MP3 supplied by the user as `WhatsApp Video 2026-07-23 at 2.49.56 PM - Trim.mp3`.
- Confirm that the group has permission to distribute both supplied assets before publishing the game.
- Godot will create import metadata in `.godot/` the first time the project is opened. That folder is intentionally excluded from the ZIP source history.
